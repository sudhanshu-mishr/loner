import { Button } from '@/components/ui/button'; import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'; import { Badge } from '@/components/ui/badge';
const plans = [['Free','$0',['Text/video matching','Basic interests','Guest mode limits','Visible reports']], ['Premium','$9',['Verified-only toggle','Precision region/language','Queue boost','Rematch recent skips','Ad-free mode']]];
export default function PricingPage() { return <main className="mx-auto max-w-6xl px-4 py-16"><h1 className="text-center text-5xl font-bold">Simple plans for safer discovery</h1><div className="mt-10 grid gap-6 md:grid-cols-2">{plans.map(([name, price, features]) => <Card key={name as string} className={name==='Premium'?'border-primary shadow-glow':''}><CardHeader><div className="flex justify-between"><CardTitle>{name as string}</CardTitle>{name==='Premium' && <Badge tone="safe">Best value</Badge>}</div><p className="text-4xl font-bold">{price as string}<span className="text-base text-muted-foreground">/mo</span></p></CardHeader><CardContent className="space-y-4"><ul className="space-y-2 text-sm text-muted-foreground">{(features as string[]).map((f) => <li key={f}>• {f}</li>)}</ul><Button className="w-full">{name==='Premium'?'Start premium':'Continue free'}</Button></CardContent></Card>)}</div></main>; }
