import { ShieldCheck } from 'lucide-react';
export default function SafetyCenter() { const items = ['Report and block are always visible during sessions.', 'Loners does not record random chats by default.', 'Moderation is strict because random chat products can attract abuse if poorly governed.', 'Never share phone numbers, addresses, socials, payment info, or private images.', 'Use Leave now if you feel uncomfortable.']; return <main className="mx-auto max-w-4xl px-4 py-12"><ShieldCheck className="mb-4 size-10 text-primary" /><h1 className="text-4xl font-bold">Safety center</h1><p className="mt-4 text-muted-foreground">How Loners protects spontaneous conversations.</p><div className="mt-8 space-y-3">{items.map((item) => <div key={item} className="rounded-3xl border border-border bg-card p-5">{item}</div>)}</div></main>; }
