import { Button } from '@/components/ui/button'; import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'; import { Switch } from '@/components/ui/switch'; import { ThemeToggle } from '@/components/ui/theme-toggle';
const settings = ['Private profile while chatting','Verified users only','Safe mode: text first','Session feedback prompts','Email safety notifications'];
export default function SettingsPage() { return <main className="mx-auto max-w-4xl px-4 py-8"><h1 className="mb-6 text-4xl font-bold">Settings</h1><Card><CardHeader><CardTitle>Privacy and matching</CardTitle></CardHeader><CardContent className="space-y-4">{settings.map((s) => <label key={s} className="flex items-center justify-between rounded-2xl bg-secondary p-4"><span>{s}</span><Switch defaultChecked /></label>)}<div className="flex items-center justify-between rounded-2xl bg-secondary p-4"><span>Theme</span><ThemeToggle /></div><div className="rounded-2xl border border-red-400/30 p-4"><h3 className="font-semibold text-red-300">Data controls</h3><p className="text-sm text-muted-foreground">Download personal data, delete account, or clear optional chat preferences.</p><Button variant="danger" className="mt-3">Delete account</Button></div></CardContent></Card></main>; }
