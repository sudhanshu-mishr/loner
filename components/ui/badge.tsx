import { cn } from '@/lib/utils';
export function Badge({ className, tone = 'neutral', ...props }: React.HTMLAttributes<HTMLSpanElement> & { tone?: 'neutral' | 'safe' | 'warning' | 'danger' }) { return <span className={cn('inline-flex items-center rounded-full border px-3 py-1 text-xs font-medium', tone === 'safe' && 'border-emerald-400/30 bg-emerald-400/10 text-emerald-300', tone === 'warning' && 'border-amber-400/30 bg-amber-400/10 text-amber-300', tone === 'danger' && 'border-red-400/30 bg-red-400/10 text-red-300', tone === 'neutral' && 'border-white/10 bg-white/5 text-muted-foreground', className)} {...props} />; }
