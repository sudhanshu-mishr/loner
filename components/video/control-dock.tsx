'use client';
import { Mic, PhoneOff, ShieldAlert, SkipForward, Video } from 'lucide-react';
import { Button } from '@/components/ui/button';
export function ControlDock({ onMic, onCamera, onNext, onEnd }: { onMic: () => void; onCamera: () => void; onNext: () => void; onEnd: () => void }) { return <div className="fixed inset-x-4 bottom-4 z-30 mx-auto flex max-w-xl items-center justify-center gap-2 rounded-full border border-white/10 bg-background/90 p-2 shadow-2xl backdrop-blur-xl md:static md:mt-4"><Button size="icon" variant="secondary" onClick={onMic} aria-label="Mute microphone"><Mic className="size-5" /></Button><Button size="icon" variant="secondary" onClick={onCamera} aria-label="Toggle camera"><Video className="size-5" /></Button><Button variant="secondary" onClick={onNext}><SkipForward className="size-4" />Next</Button><Button variant="outline"><ShieldAlert className="size-4" />Panic</Button><Button size="icon" variant="danger" onClick={onEnd} aria-label="End session"><PhoneOff className="size-5" /></Button></div>; }
