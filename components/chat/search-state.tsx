import { Badge } from '@/components/ui/badge';
export function SearchState({ mode }: { mode: string }) { return <div className="grid min-h-[50vh] place-items-center rounded-[2rem] border border-dashed border-primary/30 bg-primary/5 p-8 text-center"><div><div className="mx-auto mb-6 size-20 animate-pulse rounded-full bg-primary/30 shadow-glow" /><Badge tone="safe">Safety matching active</Badge><h2 className="mt-4 text-3xl font-bold">Searching for a {mode.toLowerCase()} match…</h2><p className="mt-3 max-w-md text-muted-foreground">We prioritize compatible interests, language, trust pools, and recent abuse signals. Empty queues retry automatically.</p></div></div>; }
